server_host=0.0.0.0
server_port=8000
root_dir=./var
default_file=index.html

# -------------------------------------------------------
# Reverse Proxy Mode Settings (used when started with 'reverse')
# -------------------------------------------------------

# Global proxy timeouts (seconds)
backend_connect_timeout=30
backend_response_timeout=60

# Backend definitions — add one block per backend service
# Format: backend.<name>.host | backend.<name>.port | backend.<name>.path_prefix
# Routing: longest matching path_prefix wins
# Example:
backend.api.host=127.0.0.1
backend.api.port=3000
backend.api.path_prefix=/api

backend.web.host=127.0.0.1
backend.web.port=4000
backend.web.path_prefix=/
